export interface Employee {
  id: string;
  name: string;
  address: string;
  bankName: string;
  branchName: string;
  accountType: string; // 普通・当座など
  accountNumber: string;
  accountHolder: string;
}

